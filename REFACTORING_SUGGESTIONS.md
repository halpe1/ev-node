# Refactoring Suggestions — 2026-03-10

> Auto-generated by refactoring-agent. Review, edit, and close if not actionable.

## High Impact
- **[block/internal/cache/generic_cache.go]** — The `SaveToStore` method is now essential for persistence. Ensure that all shutdown paths correctly invoke `SaveToStore`, as the hot-path persistence has been removed to reduce Badger write amplification.
- **[block/internal/cache/generic_cache_test.go]** — Several tests were updated to explicitly call `SaveToStore` to simulate clean shutdowns. Verify that all future tests modifying cache state maintain this pattern to avoid flaky state restoration tests.

## Medium Impact
- **[node/execution_test.go, pkg/da/types/namespace_test.go]** — Minor cleanup: redundant `t.Fatal` blocks were identified that should be followed by a `return` to ensure test execution stops correctly.

## Low Impact / Nice-to-haves
- **[.github/workflows/docker-build-push.yml]** — Reverted `docker/login-action` to v3 while upgrading `docker/setup-buildx-action` to v4. Ensure this inconsistency in action versions is intentional and meets organizational security requirements.

## Out of Scope
- Dependency version bumps (handled via automated PRs).
- CI workflow structural changes.
